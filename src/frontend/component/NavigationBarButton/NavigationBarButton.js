import React from 'react';
import { Button } from 'react-toolbox/lib/button';
import theme from './NavigationBarButton.css';

const NavigationBarButton = (props) => <Button {...props} theme={theme} />;
export default NavigationBarButton;
